describe("Deve demonstrar o uso do Jasmine no NodeJs", () => {
	it("deve demonstrar o primeiro uso" , () => {
		expect(true).toEqual(true);
	});

	it("deve demonstrar o uso do karma em modo de escuta", () => {
		expect(1).toEqual(1);
	});
});